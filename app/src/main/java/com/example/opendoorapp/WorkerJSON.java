package com.example.opendoorapp;

import android.content.Context;

public class WorkerJSON {

  public WorkerJSON(Context context) {

    if(JSON.loadJSON(context, Workers.class, "Benjamin.json").equals())
  }
}
